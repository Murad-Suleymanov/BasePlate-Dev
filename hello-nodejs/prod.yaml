routes:
  - main                       # normal route (15s) — also the pool's primary route
  - main-long-timeout          # same pool, separate HTTPRoute with a 120s timeout
traffic:
  provider: istio
  ejectUnhealthy: false
  latencyAware: true

singleton: true
maxDown: 1
